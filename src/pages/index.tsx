import { Home } from "../components/home";

// const fetcher = (...args) => fetch(...args).then((res) => res.json());

const Page = () => {
  return <Home />;
};
export default Page;

// export default Page;
